#Сначала игнорировать ВСЁ в корне (/*)
/*

#Символ ! в начале строки означает "не игнорировать" этот каталог
#Символ / в начале пути означает, что правило применяется только в корне проекта

# Игнорирование стандартных каталогов Python
__pycache__/  # Кэш-файлы Python (автоматически генерируемые)
*.py[cod]     # Другие файлы кэша Python
*.so          # Скомпилированные расширения Python

# Игнорирование каталогов проекта
!/app/
!/config/
!/database/
!/static/
!/resources/
!/tests/

# Документация - НЕ игнорировать


# Не игнорировать основные файлы проекта
!/craft.py
!/run.py
!/requirements.txt
!/package.json
!/webpack.config.js


!/alembic.ini
!/.env.example

# Файлы окружения и конфигурации
.env          # Файл с переменными окружения (никогда не должен попадать в репозиторий!)
.env.*        # Все варианты файлов окружения

# Виртуальные окружения
/node_modules
/storage
/venv/        # Виртуальное окружение (обычное расположение)
venv*/        # Все варианты каталогов виртуального окружения
.python-version  # Файл указания версии Python (если используется pyenv)

# Node.js зависимости
/node_modules/  # Каталог зависимостей Node.js (если проект использует фронтенд)

# Прочие файлы
.DS_Store     # Служебные файлы macOS
Thumbs.db     # Служебные файлы Windows
